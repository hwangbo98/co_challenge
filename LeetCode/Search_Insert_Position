class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        
        
        for key, value in enumerate(nums) : 
            if value >= target : 
                return key
        return len(nums)